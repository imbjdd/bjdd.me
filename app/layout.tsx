import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Salim Boujaddi',
  description: 'Web3 enthusiast with interest in Mathematics and CS. I\'m currently a student looking for a start-up idea. You can connect with me on Linkedin and chat with me on Discord.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
